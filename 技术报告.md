# 脑机接口算法任务技术报告

## 一、项目背景

### 1.1 SSVEP 原理简介

稳态视觉诱发电位（Steady-State Visual Evoked Potential，SSVEP）是一种常见的脑机接口范式。当人眼注视一个以特定频率闪烁的视觉刺激时，大脑枕叶视觉皮层会产生相同频率的脑电信号响应。通过分析脑电信号中的频率成分，可以反推出用户正在注视哪个刺激目标，从而实现对计算机或外部设备的控制。

**一个形象的比喻**：就像 tuning fork（音叉）——一个 8Hz 的视觉刺激，会让大脑产生一个 8Hz 的"回响"。我们通过分析脑电波里有什么频率，就知道用户在盯着哪个闪烁的方块。

### 1.2 比赛任务说明

本赛题提供 8 个不同频率（8、9、10、11、12、13、14、15Hz）的视觉刺激，每个 trial 持续 4 秒，共 48 个 trial。任务是从 6 通道的 EEG 信号中，准确判断每个 trial 对应的是哪个刺激频率。

官方提供的示例代码使用 CCA（典型相关分析）算法，在示例数据 D2.csv 上基线准确率约为 56%。

---

## 二、原始代码分析

### 2.1 代码结构

原始代码由两部分组成：

**ssvepdetect.py** — SSVEP 检测器类
- `__init__`：构造 8 个频率的参考信号模板
- `pre_filter`：50Hz 陷波 + 6-90Hz 带通滤波
- `detect`：对预处理后的信号做 CCA 分析，返回预测结果

**demo.py** — 运行入口
- 读取 CSV 数据文件
- 按 4 秒一段切分为 48 个 epoch
- 调用检测器逐个预测
- 对比真值计算准确率

### 2.2 核心算法：CCA

CCA（Canonical Correlation Analysis，典型相关分析）是一种多变量统计方法，用于衡量两组变量之间的相关性。

在 SSVEP 检测中的具体用法：
- 第一组变量：实际采集的 EEG 信号（6 通道 × 1000 采样点）
- 第二组变量：构造的参考信号（2 行：sin 和 cos 波）
- CCA 计算这两组变量之间的最大相关系数，相关系数越大，说明 EEG 信号与该频率越匹配

**对每个刺激频率都做一次 CCA，取相关系数最大的频率作为检测结果。**

### 2.3 原始代码的严重缺陷

经过仔细分析，原始代码存在一个**关键 bug**：

```python
# 原始代码第20行
costemp = np.sin(_)    # BUG：应该是 np.cos(_)
```

正弦（sin）和余弦（cos）本应是正交的参考信号对，用于覆盖信号的不同相位。但这里两行都是 sin，导致参考信号矩阵的两行完全相同（秩亏），CCA 的参考信号实际上只有 1 个独立分量。这严重影响了 CCA 的判别能力。

**定量分析**：
- 正常 sin-cos 对的相关系数：0.000（正交，完美）
- 原始 sin-sin 对的相关系数：1.000（完全重复，秩亏）

**这种秩亏的参考信号导致**：
1. CCA 数值解不稳定
2. 对高质量数据（D2）反而表现更差（56%），因为通道间高相关性与秩亏参考相互作用产生更多随机错误
3. 对低质量数据（D1）相对"好"一些（65%），因为死通道无意中起到了正则化作用

---

## 三、优化过程

### 3.1 第一步：修复 cos bug（准确率 +23%）

**发现**：原始代码中 `costemp = np.sin(_)` 应为 `np.cos(_)`。

**修改**：
```python
# 修改前
sintemp = np.sin(_)
costemp = np.sin(_)    # BUG

# 修改后
sintemp = np.sin(_)
costemp = np.cos(_)    # 修复
```

**效果**：仅修复这一个字符，D1 准确率从 65% 提升至约 79%，说明正交参考信号对 CCA 至关重要。

**原理**：sin 和 cos 构成正交基，可以覆盖任意相位的 SSVEP 响应（因为 sin(ωt+φ) = sin(ωt)cos(φ) + cos(ωt)sin(φ)）。只用 sin 相当于只能匹配相位为 0 的信号，会漏掉实际存在的相位偏移。

### 3.2 第二步：加入谐波参考信号（准确率 79% → 92%）

**问题**：只用基频（8-15Hz）作为参考，丢失了谐波中的判别信息。

**方案**：在参考信号中加入 2 次和 3 次谐波。

```python
# 对每个刺激频率，构造 3 组 sin-cos 对（共 6 行参考信号）
for h in range(1, 4):  # h=1,2,3 分别对应基频、2次谐波、3次谐波
    ref_signals.append(np.sin(2 * π * freq * h * t))
    ref_signals.append(np.cos(2 * π * freq * h * t))
```

**原理**：SSVEP 响应不仅包含基频成分，还包含谐波成分。例如 8Hz 刺激不仅会产生 8Hz 信号，还会产生 16Hz、24Hz 等谐波。3 次谐波覆盖到 8-15Hz 的 2-3 倍频（最高 45Hz），大幅丰富了 CCA 的判别特征。

**为什么不是越多越好**：4 次谐波反而略有下降，因为谐波过多会引入高频噪声的干扰。3 次谐波是在本数据集上的最优折中。

### 3.3 第三步：FBCCA 滤波器组 CCA（准确率 92% → 96%）

**问题**：单频段 CCA 对所有频率成分一视同仁，无法充分利用不同频段的信息。

**方案**：引入 FBCCA（Filter Bank CCA），这是 SSVEP 检测领域的经典改进方法（Chen et al. 2015）。

FBCCA 的核心思想：将信号通过多个不同频段的带通滤波器，对每个频段分别做 CCA，然后将结果加权融合。

**本实现使用的 4 个子带和权重**：

| 子带 | 频段 | 权重 | 作用 |
|------|------|------|------|
| 1 | 6-90 Hz | 1.250 | 包含所有基频和低次谐波 |
| 2 | 14-90 Hz | 0.670 | 滤除 8-13Hz 基频，保留谐波 |
| 3 | 22-90 Hz | 0.490 | 滤除 8-21Hz 成分，保留高次谐波 |
| 4 | 30-90 Hz | 0.400 | 仅保留高次谐波，增强高频分辨力 |

**加权公式**：w(i) = i^(-1.25) + 0.25

**原理**：不同频率的谐波模式不同——8Hz 的 2 次谐波是 16Hz，而 12Hz 的 2 次谐波是 24Hz。通过多个子带滤波，FBCCA 可以同时利用基频和谐波信息，获得更稳定的判别结果。

### 3.4 第四步：去趋势 + 更激进的高通滤波（纠正个别噪声 Trial）

**问题**：D1.csv 数据中存在强烈的低频噪声（0-2Hz 幅度可达数千，而 SSVEP 信号幅度仅 10-30）。原始 6Hz 高通无法完全滤除。

**方案**：
1. 添加线性去趋势（`scipy.signal.detrend`），消除每个 trial 内的基线漂移
2. 将高通从 6Hz 提升到 7Hz（通带 7-90Hz，阻带 3-90Hz），更彻底地压制低频噪声的同时不影响最低刺激频率 8Hz

**注意**：7Hz 高通对 8Hz 基频的衰减小于 0.5dB，影响微乎其微。

### 3.5 第五步：自动通道选择（准确率 96% → 98%）

**问题**：D1.csv 的通道 2（第 2 个通道）标准差仅 0.96，而其他通道标准差在 6.8-13.2 之间。这是一个近乎"死掉"的通道，贡献的只有噪声。

**方案**：自动选择方差最大的 3 个通道参与 CCA 分析。

```python
stds = data.std(axis=1)
best = np.argsort(stds)[-3:]  # 选择方差最大的3个通道
```

**原理**：方差极低的通道基本上不包含脑电信号（可能是电极接触不良或参考通道），参与 CCA 只会引入噪声。自动选择高方差通道既能排除死通道，又无需手动指定。

**为什么是 3 个**：经测试，3 通道在 D1（98%）和 D2（100%）上均表现最优。通道太少损失信号，通道太多引入噪声。

---

## 四、实验结果

### 4.1 各优化步骤准确率对比

| 步骤 | D1 准确率 | D2 准确率 | 累计提升 |
|------|----------|----------|---------|
| 原始代码（含 bug） | 65% | 56% | - |
| 修复 cos bug | ~79% | — | +~23% |
| +3 次谐波 | ~92% | — | +~13% |
| +FBCCA 4 子带 | 96% | 100% | +4% |
| +去趋势 + 7Hz 高通 | 96% | 100% | — |
| +自动选通道 | **98%** | **100%** | +2% |

### 4.2 错误分析

最终版本在 D1 上仍有 1 个错误（Trial 21：9Hz 误判为 11Hz）。经频谱分析，该 trial 的 9Hz 信噪比仅 1.37（信号功率仅比噪声高 37%），且 9Hz 的 CCA 相关系数（0.83）远低于 11Hz（0.98），表明该 trial 的 SSVEP 响应本身极弱，属于数据质量问题，非算法可解决。

### 4.3 两种数据集的差异分析

| 指标 | D1 | D2 |
|------|----|----|
| 通道方差 | 高（存在死通道） | 均匀 |
| 平均信噪比 | 1.79 | 2.27 |
| 最佳准确率 | **98%** | **100%** |

D1 整体噪声更大且含死通道，但自动通道选择策略成功适应了这种差异。

---

## 五、核心算法原理解析（小白版）

### CCA 是什么？

想象你有两组数据：一组是 EEG 信号（6 个通道），一组是参考信号（sin 波和 cos 波）。CCA 要找的答案是：

> "如何把 6 个 EEG 通道按照一定比例混合成一个信号，使得这个信号与参考信号的波形最相似？"

这个"最相似"的程度用相关系数（0-1 之间）衡量。对 8 个频率各自算一次，谁的相关性最高就选谁。

### FBCCA 是什么？

FBCCA 是 CCA 的升级版——不只在原始信号上做一次 CCA，而是先通过多个带通滤波器把信号分成不同频段，对每个频段分别做 CCA，最后加权求和。

为什么这样做？因为不同频率的"指纹"不仅体现在基频上，还体现在谐波分布上。比如 8Hz 的 2 次谐波是 16Hz，10Hz 的 2 次谐波是 20Hz——通过多个子带，FBCCA 能捕捉到这种差异。

### 为什么自动选通道有用？

SSVEP 信号主要来自枕叶视觉皮层，通常是后脑勺附近的电极信号最强。有些电极可能接触不良（死通道），或者位置不对。自动选择方差最大的通道，本质上是让算法自己"挑选"信号质量最好的电极，排除噪声大的通道。

---

## 六、代码使用说明

### 文件结构

```
Demo/
├── ssvepdetect.py    # SSVEP 检测器（含原始版和优化版）
├── demo.py           # 运行入口
└── 技术报告.md        # 本文档
```

### 版本切换

在 `ssvepdetect.py` 末尾修改版本号：

```python
VERSION = 2   # 1=原始版，2=优化版（推荐）
```

### 数据路径修改

在 `demo.py` 第 83 行修改数据路径：

```python
datapath = r'E:/.../比赛数据.csv'
```

### 运行

```bash
python demo.py
```

终端输出 48 个 trial 的预测值，按顺序填入 `result.csv` 提交即可。

---

## 七、展望与后续优化方向

1. **缩短数据窗口**：当前用 4 秒数据，可尝试 2-3 秒窗口以加快响应速度（场地任务需要），需重新调参
2. **TRCA 算法**：如果有训练数据，TRCA（Task-Related Component Analysis）通常优于 CCA
3. **自适应参数**：根据实时信号质量动态调整通道选择和子带权重
4. **集成学习**：组合多种算法（CCA + FBCCA + 频谱分析）投票，进一步提高鲁棒性
