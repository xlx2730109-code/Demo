# 脑机接口算法任务技术报告

## 一、项目背景

### 1.1 SSVEP 原理简介

稳态视觉诱发电位（Steady-State Visual Evoked Potential，SSVEP）是一种常见的脑机接口范式。当人眼注视一个以特定频率闪烁的视觉刺激时，大脑枕叶视觉皮层会产生相同频率的脑电信号响应。通过分析脑电信号中的频率成分，可以反推出用户正在注视哪个刺激目标，从而实现对计算机或外部设备的控制。

**形象比喻**：就像音叉（tuning fork）——一个 8Hz 的视觉刺激，会让大脑产生一个 8Hz 的"回响"。我们通过分析脑电波里有什么频率，就知道用户在盯着哪个闪烁的方块。

### 1.2 比赛任务说明

本赛题提供 8 个不同频率（8、9、10、11、12、13、14、15Hz）的视觉刺激，从 6 通道的 EEG 信号中准确判断每个 trial 对应的刺激频率。

**官方竞赛规格**：

| 任务 | 数据长度 | 采样点数 | 说明 |
|------|---------|---------|------|
| 练习数据（D1/D2） | 4 秒 | 1000 点 | 官方提供的示例数据 |
| Task1（正式比赛） | **3 秒** | **750 点** | 省赛任务1规格 |
| Task2（正式比赛） | **1 秒** | **250 点** | 省赛任务2规格 |

> 注意：1 秒数据仅有 250 个采样点，对算法的时间分辨率要求极高。

### 1.3 代码架构

本方案提供 **三个版本** 的检测器，通过一行代码切换：

- **V1**：原始官方代码（基线对照）
- **V2**：FBCCA 优化版（4 秒数据最优）
- **V3**：自适应版（短数据自动调整参数）

---

## 二、原始代码分析

### 2.1 代码结构

原始代码由两部分组成：

**ssvepdetect.py** — SSVEP 检测器类
- `__init__`：构造 8 个频率的 sin/cos 参考信号
- `pre_filter`：50Hz 陷波 + 6-90Hz 带通滤波
- `detect`：逐个频率做 CCA，返回相关系数最大的频率

**demo.py** — 运行入口
- 读取 CSV 数据，按 4 秒一段切分为 48 个 epoch
- 调用检测器预测，对比真值计算准确率

### 2.2 核心算法：CCA

CCA（Canonical Correlation Analysis，典型相关分析）是一种衡量两组变量之间相关性的多变量统计方法。

在 SSVEP 检测中的具体用法：
- 第一组变量：实际采集的 EEG 信号（6 通道 × 1000 采样点）
- 第二组变量：构造的参考信号（2 行：sin 和 cos 波）
- CCA 计算两组变量之间的最大相关系数，越大说明信号与该频率越匹配

对 8 个刺激频率各做一次 CCA，取相关系数最大的频率作为检测结果。

### 2.3 原始代码的严重缺陷

原始代码存在一个**关键 bug**：

```python
costemp = np.sin(_)    # BUG：应该是 np.cos(_)
```

**影响分析**：
- 正常 sin-cos 相关性：**0.000**（正交，理想参考信号）
- 原始 sin-sin 相关性：**1.000**（完全重复，参考信号秩亏）

参考信号矩阵秩亏导致：
1. CCA 数值解不稳定
2. 高质量数据（D2）准确率反而更低（56%）
3. 低质量数据（D1）中的死通道无意中部分抵消了秩亏问题（65%）
4. 这是"脏数据表现反而好"的反常现象的根源

---

## 三、优化过程（五步迭代）

### 3.1 第一步：修复 cos bug → 准确率 +23%

将 `costemp = np.sin(_)` 改为 `np.cos(_)`。sin 和 cos 构成正交基，可以覆盖任意相位的 SSVEP 响应。仅修复这一个字符，D1 准确率从 65% 提升至约 79%。

### 3.2 第二步：加入谐波参考信号 → 79% → 92%

SSVEP 响应不仅包含基频成分，还包含谐波成分。加入 2 次和 3 次谐波参考信号后，每个频率的参考信号从 2 行扩展到 6 行，大幅丰富了 CCA 的判别特征。

**为什么 3 次最佳**：经 grid search 测试（1-4 次谐波），3 次谐波在 D1/D2 上准确率最高。4 次谐波反而下降，因为高频噪声开始干扰。

### 3.3 第三步：FBCCA 滤波器组 CCA → 92% → 96%

引入 FBCCA（Filter Bank CCA, Chen et al. 2015），这是 SSVEP 检测领域的经典改进。

**核心思想**：将信号通过多个不同频段的带通滤波器，对每个频段分别做 CCA，然后加权融合。

| 子带 | 频段 | 权重 w(i) | 作用 |
|------|------|-----------|------|
| 1 | 6-90 Hz | 1.250 | 包含所有基频和低次谐波 |
| 2 | 14-90 Hz | 0.670 | 滤除 8-13Hz 基频，保留谐波信息 |
| 3 | 22-90 Hz | 0.490 | 只保留次谐波，增强高频分辨力 |
| 4 | 30-90 Hz | 0.400 | 仅保留高次谐波 |

权重公式：w(i) = i^(-1.25) + 0.25

**为什么 FBCCA 有效**：不同频率的谐波"指纹"不同——8Hz 的 2 次谐波是 16Hz，12Hz 的 2 次谐波是 24Hz。多子带滤波能同时捕捉基频和谐波的差异，比单频段 CCA 稳定得多。

### 3.4 第四步：去趋势 + 7Hz 高通 → 纠正噪声 Trial

**问题**：D1 数据中存在强烈的低频噪声（0-2Hz 幅度可达数千，SSVEP 仅 10-30），原始 6Hz 高通无法完全滤除。

**方案**：
1. 添加 `scipy.signal.detrend` 去线性漂移
2. 高通从 6Hz 提升到 7Hz（7-90Hz 通带，3-90Hz 阻带）
3. 8Hz 基频衰减小于 0.5dB，影响极小

### 3.5 第五步：自动通道选择 → 96% → 98%

**问题**：D1 的通道 2 标准差仅 0.96（其他通道 6.8-13.2），近乎"死通道"。

**方案**：自动选择方差最大的 3 个通道参与 CCA。

```python
stds = data.std(axis=1)
best = np.argsort(stds)[-3:]  # 选方差最大的3个
```

**为什么不用全部通道**：死通道不包含脑电信号，只贡献噪声。自动选高方差通道既能排除死通道，又无需手动指定。

---

## 四、V3 自适应版（针对比赛正式数据）

### 4.1 为什么需要 V3

比赛正式数据与练习数据规格不同：

| 数据 | 长度 | 最优配置 | D2 准确率 |
|------|------|---------|----------|
| 练习数据 4s | 1000 点 | 3harm-4sub | 100% |
| Task1 **3s** | **750 点** | **2harm-4sub** | **96%** |
| Task2 **1s** | **250 点** | **3harm-2sub** | **77%** |

关键发现：**3 秒数据用 2 次谐波比 3 次更好**，1 秒数据只能跑 2 个子带。

### 4.2 自适应机制

V3 在 V2 基础上增加：

1. **自适应子带数**：>500 点用 4 子带，<500 点用 2 子带，防止短数据过拟合
2. **异常值检测框架**：检测碰撞/摇晃导致的信号突变（阈值可调）
3. **多方法预计算**：PSDA 频率索引预计算，为短数据场景预留

### 4.3 三个版本准确率对比

| 版本 | D1 准确率 | D2 准确率 | 特点 |
|------|----------|----------|------|
| V1 原始官方 | 65% | 56% | cos bug，单次 CCA |
| V2 FBCCA 优化 | **98%** | **100%** | 3次谐波 + 4子带 FBCCA |
| V3 自适应 | **98%** | **100%** | = V2 + 自适应子带 + 异常检测 |

---

## 五、窗口长度与准确率的关系

### 5.1 测试结果（D2 数据）

```
2.0s (500点)  44/48 = 0.92
2.5s (625点)  47/48 = 0.98
3.0s (750点)  47/48 = 0.98
3.5s (875点)  47/48 = 0.98
4.0s (1000点) 48/48 = 1.00
```

关键结论：**2.5 秒就能达到 98%**，响应速度与准确率的最优折中点是 2.5-3 秒。

### 5.2 1 秒数据的挑战

1 秒（250 点）最优仅 77%，原因是：
- 250 点下 FFT 频率分辨率仅 1Hz（刺激频率间隔也是 1Hz）
- CCA 只有 250 个样本估计相关性，统计稳定性下降
- 滤波器的瞬态响应占据了数据的相当一部分

**对策**：1 秒数据减少子带数（2 子带）和使用 2-3 次谐波。

---
## 六、无标签自检方法（置信度指标）

### 6.1 问题

比赛测试数据没有标签（stimID 列全部显示为 -1），无法直接计算准确率。如何判断参数选得好不好？

### 6.2 置信度指标（代码自动计算，无需手动调节）

即使没有标签，算法也能通过分析 CCA 得分给自己"打分"：

```python
置信度 = (第一名得分 - 第二名得分) / 第一名得分
```

- **置信度越高** → 算法对结果越确定 → 准确率可能越高
- **置信度越低** → 算法在犹豫 → 参数可能不合适

**这个指标是代码内部自动算的，选手不需要做任何操作。** 如果在训练数据上对比发现"置信度高时准确率也高"，就说明这个指标可信。

### 6.3 分布均匀度

48 个 trial 应为均匀的 8 类（每类 6 个）。如果预测分布严重偏斜（如某类出现 15 次），说明参数有问题。这也是自动计算的。

### 6.4 测试结果对比

| 谐波数 | 置信度 | 分布均匀度 | 预测分布 |
|-------|-------|-----------|---------|
| 1 | 0.314 | 0.38 | [2,3,7,6,**15**,5,6,4] ← 偏斜 |
| 2 | **0.476** | **0.86** | [5,6,6,6,8,6,6,5] ✓ |
| 3 | 0.392 | **0.80** | [5,6,6,6,9,5,6,5] ✓ |
| 4 | 0.380 | 0.76 | [4,7,6,6,9,5,6,5] ✓ |

**结论**：谐波 2 和 3 置信度高且分布均匀，都可选。最终选择 3 次谐波（经有标签数据验证）。

---

## 七、核心算法原理解析

### CCA 是什么？

想象有两组数据：EEG 信号（6 通道）和参考信号（sin/cos 波）。CCA 要找到：

> "如何把 6 个 EEG 通道按一定比例混合成一个信号，使这个信号与参考信号波形最相似？"

"最相似"程度用相关系数衡量。对 8 个频率各算一次，最高的就是答案。

### FBCCA 是什么？

FBCCA 是 CCA 的升级版——不只在原始信号上做一次 CCA，而是：
1. 把信号通过多个带通滤波器分成不同频段
2. 对每个频段分别做 CCA
3. 按权重融合结果

不同频率的"指纹"不仅体现在基频上，还体现在谐波分布上。8Hz 的 2 次谐波是 16Hz，12Hz 的 2 次谐波是 24Hz——多子带能捕捉这种差异。

### 为什么自动选通道有用？

SSVEP 信号来自枕叶视觉皮层。有些电极可能接触不良（死通道），或受眼动/肌电干扰。自动选方差最大的通道，本质是让算法自己挑选信号质量最好的电极。

### PSDA 是什么？

PSDA（Power Spectral Density Analysis）是把时域信号变换到频域，直接看 8-15Hz 各频率的功率大小。功率最高的频率就是检测结果。这是最直观的 SSVEP 检测方法，但对短数据效果有限。

### 三种方法的比较

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| CCA | 时域匹配，统计稳健 | 需要足够采样点 | 长数据（3-4s） |
| FBCCA | 多频段，抗干扰强 | 计算量较大 | 各种长度，推荐 |
| PSDA | 直观，速度快 | 频率分辨率受限 | 辅助判断 |

---

## 八、可能遇到的问题

### 8.1 数据格式说明（已确认）

根据组委会说明，正式比赛数据与练习数据格式一致：

| 项目 | 规格 |
|------|------|
| 采样率 | **250Hz 固定不变** |
| 通道数 | **6 通道固定不变** |
| 数据长度（Task1） | **3 秒 / 750 点** |
| 数据长度（Task2） | **1 秒 / 250 点** |
| stimID 列 | 隐藏，全部显示为 **-1**（无标签） |
| 列数 | 与练习数据一致 |

> 注：正式数据需要选手自行将 demo.py 中的 `dataLen` 改为 3（Task1）或 1（Task2）。

### 8.2 scipy/sklearn 版本兼容性

| 问题 | 概率 | 后果 |
|------|------|------|
| scipy/sklearn 版本不兼容 | 低 | 函数报错 |

### 8.3 场地任务干扰因素

| 类型 | 因素 | 影响程度 |
|------|------|---------|
| 操作者 | 眨眼、走神、疲劳 | 大 |
| 环境 | 屏幕刷新率（需 120Hz+） | 大 |
| 环境 | 电磁干扰、灯光频闪 | 中 |
| 设备 | 电极佩戴质量 | 大 |

---

## 九、代码使用说明

### 文件结构

```
Demo/
├── ssvepdetect.py    # SSVEP 检测器（V1/V2/V3 三个版本）
├── demo.py           # 运行入口，含窗口对比测试
└── 技术报告.md        # 本文档
```

### 版本切换

`ssvepdetect.py` 末尾：

```python
VERSION = 3   # 1=原始版, 2=FBCCA优化版, 3=自适应版（推荐）
```

### 数据路径

`demo.py` 中修改 `datapath` 为实际数据路径。

### 运行

```bash
python demo.py
```

终端输出 48 个 trial 的预测值，按顺序填入 `result.csv` 提交。

---

## 十、后续优化方向

1. **TRCA 算法**：如有训练数据，TRCA（Task-Related Component Analysis）通常优于 CCA
2. **异常值处理调优**：根据比赛数据调整碰撞/摇晃检测阈值
3. **多方法集成**：CCA + FBCCA + PSDA 投票，进一步提高鲁棒性
4. **滑动窗口策略**：短数据下用重叠窗口提升稳定性
5. **交叉验证**：训练数据上做交叉验证，防止过拟合
